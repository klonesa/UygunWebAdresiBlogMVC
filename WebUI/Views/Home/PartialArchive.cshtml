@using WebUI.Helpers
@model IEnumerable<WebUI.Data.Entities.Archives>
@foreach (var item in Model)
{
    <a class="dropdown-item" href="/arsiv/@SeoHelper.ToSeoUrl(item.ArchiveName).ToLower()-@item.ArchiveId">@item.ArchiveName</a>
}

