@using WebUI.Helpers
@model IEnumerable<WebUI.Data.Entities.Categories>
@foreach (var item in Model)
{
    <a class="dropdown-item" href="/kategori/@SeoHelper.ToSeoUrl(item.CategoryName).ToLower()-@item.CategoryId">@item.CategoryName</a>
}
